function doPost(e) { var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('Registros') || SpreadsheetApp.getActiveSpreadsheet().insertSheet('Registros'); var data = JSON.parse(e.postData.contents); if (sheet.getLastRow() === 0) sheet.appendRow(['Fecha','Nombre','Email','WhatsApp']); sheet.appendRow([new Date(), data.name || '', data.email || '', data.whatsapp || '']); return ContentService.createTextOutput(JSON.stringify({ ok: true })).setMimeType(ContentService.MimeType.JSON); }
