const SHEET_NAME="Registros";function doPost(e){try{const data=JSON.parse(e.postData.contents);const sheet=SpreadsheetApp.getActiveSpreadsheet().getSheetByName(SHEET_NAME);if(!sheet)throw new Error("No existe la hoja: "+SHEET_NAME);sheet.appendRow([new Date(),data.name||"",data.email||"",data.whatsapp||"",data.event||"",data.source||"Landing Web",data.status||"Nuevo"]);return ContentService.createTextOutput(JSON.stringify({ok:true})).setMimeType(ContentService.MimeType.JSON)}catch(err){return ContentService.createTextOutput(JSON.stringify({ok:false,error:String(err)})).setMimeType(ContentService.MimeType.JSON)}}